/// <reference types="vite/client" />

// Injected by Vite `define` — a unique id per build (service-worker cache versioning).
declare const __BUILD_ID__: string
// Injected by Vite `define` — the git short hash, shown in Settings as the visible build marker.
declare const __BUILD_COMMIT__: string
