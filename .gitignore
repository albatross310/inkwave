node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example

# Browser-automation / manual test artifacts (screenshots live at repo root)
.playwright-mcp/
/*.png

# React Router framework generated types
.react-router/
/build/

# Internal security audit — keep out of the repo (git add -f to commit deliberately)
PROVENANCE-SECURITY-AUDIT.md
SECURITY-AUDIT-2026-06.md

# Claude Code local worktrees / session data
.claude/
.vercel

# bench harness: built vendor bundle (rebuild from tiptap-src) + scratch
scripts/bench/static/tiptap/
scripts/bench/dbg*.mjs
scripts/scrub-probe/results-*.json
scratch-shots/
